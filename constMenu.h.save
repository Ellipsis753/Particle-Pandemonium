#pragma once

#include "Copengl.h"

/*
The positions and dimensions of each of the buttons on the menu screen
*/

namespace MENU {
    namespace START_GAME {
        const float X = Copengl::convert_to_opengl_x(118);
        const float Y = Copengl::convert_to_opengl_y(191);
        const float W = Copengl::convert_pixel_width(364);
        const float H = Copengl::convert_pixel_height(71);
    }
    namespace TUTORIAL {
        const float X = Copengl::convert_to_opengl_x(116);
        const float Y = Copengl::convert_to_opengl_y(324);
        const float W = Copengl::convert_pixel_width(365);
        const float H = Copengl::convert_pixel_height(75);
    }
    namespace QUIT_GAME {
        const float X = Copengl::convert_to_opengl_x(184);
        const float Y = Copengl::convert_to_opengl_y(450);
        const float W = Copengl::convert_pixel_width(221);
        const float H = Copengl::convert_pixel_height(72);
    }
}
