#include "Cbullet.h"

Cbullet::Cbullet(float x, float y, float xSpeed, float ySpeed): x(x), y(y), xSpeed(xSpeed), ySpeed(ySpeed) {
    //empty
}

float Cbullet::get_x() {
    return x;
}

float Cbullet::get_y() {
    return y;
}
